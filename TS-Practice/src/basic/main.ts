// basic types

let myName: string = 'Magnus';

let myAide: string;

myAide = 'Maruc';
myAide = 'Lepidus';

let meaningOfLife: number ;
let isLoading: boolean;

meaningOfLife = 42;
isLoading = true;

let album: string | number;
album = 'Cyberpunk 2077';

const sum = (a:number, b:number) => {
    return a + b;
};

let postId: string | number;
let isActive: number | boolean | string;

let re:RegExp = /\w+/g;