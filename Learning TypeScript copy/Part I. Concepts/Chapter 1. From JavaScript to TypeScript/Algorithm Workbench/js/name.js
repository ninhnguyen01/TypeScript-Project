// define variable
var firstName = 'Georgia'; // specify that the value is a string
var nameLength = firstName.length; // typescript 'length' function is different from javascript
// debug check
console.log(firstName);
console.log(nameLength);
console.log(firstName, "is a length of", String(nameLength));
