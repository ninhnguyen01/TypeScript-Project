let rocker; // Type: any

rocker = "joan jett"; // Type: string

// debug check
console.log(rocker)

// debug check
console.log(rocker.toUpperCase()); // Ok

rocker = 19.58; // Type: number

// debug check
console.log(rocker)

// debug check
console.log(rocker.toPrecision(2)); // Ok
