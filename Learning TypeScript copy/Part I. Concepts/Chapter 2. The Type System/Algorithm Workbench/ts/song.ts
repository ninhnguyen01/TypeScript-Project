// define variable with tenary operator '?'
let bestSong = Math.random() > 0.5 // math module
  ? "Chain of Fools" // option 1
  : "Respect"; // option 2

console.log(bestSong)
console.log(typeof(bestSong))
